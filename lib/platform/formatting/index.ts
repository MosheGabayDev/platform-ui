/**
 * @module lib/platform/formatting
 * Cross-platform formatting utilities (date, number, currency).
 * @platform cross
 */
export {
  formatDate,
  formatDateTime,
  formatRelativeTime,
  formatNumber,
  formatCurrency,
  formatBytes,
} from "./format";
